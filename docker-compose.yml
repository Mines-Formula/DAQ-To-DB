services:
  web:
    build: .
    container_name: daq-to-db
    ports:
      - "6969:6969"
    volumes:
      - ./app/data:/app/data
      - ./app/templates:/app/templates
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
