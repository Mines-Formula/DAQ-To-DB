services:
  web:
    build:
      context: ./src
    network_mode: host
    ports: !reset []
    stop_signal: SIGINT
    restart: unless-stopped
    volumes:
      - ./data:/data
    environment:
      - FLASK_ENV=production
      - DATA_DIR=/data
